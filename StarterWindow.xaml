<Window x:Class="DeskPokemon.StarterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DeskPokemon - 스타팅 선택"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ResizeMode="NoResize" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        RenderOptions.BitmapScalingMode="NearestNeighbor">
  <Window.Resources>
    <!-- 후보 카드. 메인 창 탭과 같은 룩. -->
    <Style x:Key="Choice" TargetType="Button">
      <Setter Property="Margin" Value="4,0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd" CornerRadius="6" Padding="8,6" Background="#33FFFFFF" BorderThickness="2" BorderBrush="Transparent">
              <ContentPresenter/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#55FFFFFF"/>
                <Setter TargetName="Bd" Property="BorderBrush" Value="#FFD84A"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Border Background="#E6202020" CornerRadius="10" Padding="12,10" MouseLeftButtonDown="OnDrag">
    <StackPanel>
      <Grid Margin="0,0,0,8">
        <TextBlock Text="스타팅 포켓몬을 골라줘" Foreground="White" FontWeight="Bold" FontSize="14"
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center" Click="OnClose"
                Foreground="#DDDDDD" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="4,0"
                ToolTip="종료"/>
      </Grid>
      <!-- 후보 카드(아이콘 + 이름 + 타입)는 코드에서 채움 -->
      <StackPanel x:Name="Choices" Orientation="Horizontal" HorizontalAlignment="Center"/>
    </StackPanel>
  </Border>
</Window>
